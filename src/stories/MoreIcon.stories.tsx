import { Meta, StoryObj } from '@storybook/react';
import MoreIcon from './MoreIcon';

const meta: Meta<typeof MoreIcon> = {
    title: 'Icons/MoreIcon',
    component: MoreIcon,
    };

export default meta;

export const Default: StoryObj<typeof MoreIcon> = {};